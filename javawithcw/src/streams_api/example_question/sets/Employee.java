package streams_api.example_question.sets;

public class Employee {
    String employeeName;                                    // Stores the employee name
    Double salary;                                          // Stores the salary of Employee

    // Employee Class Constructor to initialize the instance variable
    public Employee(String employeeName, Double salary) {
        this.employeeName = employeeName;
        this.salary = salary;
    }
}
